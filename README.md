# aws-deployment-platform
A complete system where pushing code to GitHub automatically provisions cloud infrastructure, builds and pushes a Docker image, deploys it to AWS, runs health checks, and sends a notification with full monitoring and zero manual steps.
